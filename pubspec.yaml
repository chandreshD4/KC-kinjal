name: kc_kinjal
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  http: ^1.2.0
  permission_handler: ^11.3.0
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.0
  device_info_plus: ^10.1.0
  # त्रुटियों को जड़ से खत्म करने के लिए एकदम स्टेबल वर्जन
  camera: 0.10.6
  flutter_tts: 3.8.5
  speech_to_text: ^6.6.0
  google_generative_ai: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profile.png
    - assets/raju_bhai.mp3
