name: kc_aradhana
description: "A VIP YouTube MP3 Downloader App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  permission_handler: ^11.0.1
  dio: ^5.4.0
  audioplayers: 5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profile.png
    - assets/raju_bhai.mp3

dependencies:
  shared_preferences: ^2.2.0
  device_info_plus: ^10.1.0
